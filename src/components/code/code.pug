
mixin code(options)
  -
    const label = options.label ?? ''
    const modify = options.modify ?? ''
    const addClass = options.class ?? ''
    const flLabel = options.flLabel ?? false
    const classComp = 'code'
    let className = classComp
    const codeTop = '$(\'.demo\').RangeSliderFox({';
    const codeBottom = '})';
    if(modify){
    className += ` ${classComp}_${modify}`}
    if(addClass){
    className += ` ${addClass}`}

  fieldset(class=`${className} js-${classComp}`)
    - if(!flLabel){
    legend(class=`${classComp}__label`)= label
    -}
    
    div(class=`${classComp}__config`)
      p(class=`${classComp}__lang-js`)=codeTop
      ul(class=`${classComp}__options js-${classComp}__options`)
      p(class=`${classComp}__lang-js`)=codeBottom
    button(class=`${classComp}__copy js-${classComp}__copy`) Copy code